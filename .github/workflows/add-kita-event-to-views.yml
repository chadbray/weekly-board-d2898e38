name: Add Kita Elternabend event to all views

on:
  push:
    paths:
      - kita-elternabend-update-trigger.txt

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Add event to dashboard views
        run: |
          python - <<'PY'
          from pathlib import Path
          event = "ONCE.push({date:'2026-10-07',title:'Kita VL & Elternabend',person:'josie',timeLabel:'18:00'});"
          marker = "if(!await unlockCalendar())return;"
          for name in ('index.html', 'landscape.html'):
              p = Path(name)
              s = p.read_text()
              if event not in s:
                  if marker not in s:
                      raise SystemExit(f'Unlock marker not found in {name}')
                  p.write_text(s.replace(marker, marker + event, 1))
          PY
      - name: Commit dashboard views
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html landscape.html
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Add Kita VL and Elternabend for 7 Oct 2026"
            git push
          fi
