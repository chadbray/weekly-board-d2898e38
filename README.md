# Encrypted dashboard

This public repository contains only the dashboard shell and AES-256-GCM encrypted calendar data. The access key is not stored here.

Search-engine indexing is disabled. Authorised maintenance uses the separate private source repository and recovery record.
